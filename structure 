openstudent/
│
├── index.html                  # Landing page (public)
├── dashboard.html              # Authenticated user dashboard
├── upload.html                 # Document upload interface
├── search.html                 # Full-text search interface
│
├── assets/
│   ├── css/
│   │   ├── base.css            # Global styles (reset, typography)
│   │   ├── auth.css            # Login/signup styles
│   │   ├── dashboard.css       # Dashboard-specific styles
│   │   ├── upload.css          # Upload form styles
│   │   └── search.css          # Search results styling
│   │
│   ├── js/
│   │   ├── config.js           # Supabase + ImageKit keys (non-sensitive public config)
│   │   ├── auth.js             # Sign-up, sign-in, sign-out, auth state listener
│   │   ├── storage.js          # File upload + metadata insert
│   │   ├── search.js           # FTS logic and rendering
│   │   ├── ui.js               # DOM manipulation helpers (show/hide, alerts, etc.)
│   │   └── imagekit.js         # Thumbnail generation and transformations
│   │
│   └── img/
│       └── logo.svg            # Static assets (fallbacks, logos, etc.)
│
├── supabase/
│   ├── schema.sql              # Table definitions
│   ├── policies.sql            # RLS policies
│   └── seed.sql                # Optional seed data for dev
│
├── docs/                       # Project documentation
│   ├── README.md               # Overview and setup
│   ├── architecture.md         # System design and NFRs
│   ├── onboarding.md           # Contributor guide
│   └── roadmap.md              # Feature roadmap
│
└── .github/
    └── workflows/
        └── deploy.yml          # GitHub Pages CI/CD (optional)


OpenStudent/
├── public/                         ← Public-facing site
│   ├── index.html                  ← Landing page (intro + featured projects)
│   ├── projects.html               ← Full archive with tabs and filters
│   ├── project.html                ← Single project view (optional)
│   ├── blog/
│   │   ├── index.html              ← Blog post listing
│   │   └── post.html               ← Individual blog post view
│   ├── contribute.html             ← Contributor submission form (future)
│   └── thankyou.html               ← Confirmation after submission
│
├── admin/                          ← Admin-only dashboard
│   ├── login.html                  ← Supabase Auth login
│   ├── dashboard.html              ← Overview of metrics and recent uploads
│   ├── add_project.html            ← Form to submit new project metadata
│   ├── edit_project.html           ← Update or delete existing projects
│   └── blog_editor.html            ← Markdown editor for writing blog posts
│
├── assets/                         ← Static resources
│   ├── css/
│   │   └── style.css               ← Shared styling
│   ├── js/
│   │   ├── supabase.js             ← Supabase client setup
│   │   ├── projects.js             ← Logic for filtering and rendering projects
│   │   ├── admin.js                ← Logic for admin form handling
│   │   └── blog.js                 ← Blog rendering and editor logic
│   └── images/                     ← Local fallback images (optional)
│
└── README.md                       ← Project documentation
